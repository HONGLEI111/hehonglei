.card-social .social-item:hover .social-inner {
transform: rotateY(180deg);
}

.social-inner {
transform-style: preserve-3d;
transition: transform 0.6s ease;
}

.social-front,
.social-back {
backface-visibility: hidden;
}

.social-back {
transform: rotateY(180deg);
}
